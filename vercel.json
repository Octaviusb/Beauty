{
  "version": 2,
  "builds": [
    {
      "src": "server/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/products", "dest": "server/server.js" },
    { "src": "/api/wompi-link", "dest": "server/server.js" }
  ]
}
